name: Update to README

on:
  push:
    branches: [ "main" ]

jobs:
  change-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Insert Project Tree
        uses: smoothbear/project-tree-action@0.0.6
        with:
          email: ${{ secrets.GIT_EMAIL }}
          username: smoothbear
