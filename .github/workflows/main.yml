name: Update to README

on:
  push:
    branches: [ "main" ]

jobs:
  change-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Insert Project Tree
        uses: smoothbear/project-tree-action@da40d2298451b4dccd0c443b648c162da9c4eb48
        with:
          email: ${{ secrets.GIT_EMAIL }}
          username: smoothbear
